<div class="container">
  <div class="row">
    <div class="col-md-10 mx-auto mt-3">
      <h2>Follow Users</h2>
        <% if @users.exists? %>
          <%= render 'user/relationships/follow', user: @user, users: @users %>
        <% else %>
        <p>フォロー中のユーザーはいません</p>
        <% end %>
      </div>
  </div>
</div>